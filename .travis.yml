language: ruby
rvm:
  - 2.1.1
script:
  - bundle exec jekyll build && bundle exec htmlproof ./_site --only-4xx --disable-external
env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of html-proofer
